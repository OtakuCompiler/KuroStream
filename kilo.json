{
  "$schema": "https://app.kilo.ai/config.json",
  "instructions": ["AGENTS.md", "web.md"],
  "mcp": {
    "firebase": {
      "type": "local",
      "command": ["firebase-mcp"],
      "enabled": true,
      "timeout": 30000
    }
  }
}
